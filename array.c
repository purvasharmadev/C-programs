#include <stdio.h>
#include <stdlib.h>

/*
ARRAY: to store a list of something.
    we give datatype for array following [] = {}
    comma seperate data/element in array.
    we can access any element of array by defining there index i.e 0,1,2,3..
    an index in array starts with 0.
    we can edit element in array through there index



 */


int main()
{
    int phoneNum[10];
    int i;
    printf("Enter your Phone Number");
    for(i=0;i<10;i++){
        scanf("%d",phoneNum[i]);
        printf(i)
    }

}
